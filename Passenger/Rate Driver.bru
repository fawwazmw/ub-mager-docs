meta {
  name: Rate Driver
  type: http
  seq: 8
}

post {
  url: {{base_url}}/rides/:id/rate
  body: json
  auth: inherit
}

body:json {
  {
    "rating": 5,
    "comment": "Great driver, very polite!"
  }
}
