meta {
  name: Get Driver Rides
  type: http
  seq: 10
}

get {
  url: {{base_url}}/admin/drivers/:id/rides?limit=5
  body: none
  auth: inherit
}

params:path {
  id: {{driver_id}}
}

params:query {
  limit: 5
}
