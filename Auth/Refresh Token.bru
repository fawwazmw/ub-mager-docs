meta {
  name: Refresh Token
  type: http
  seq: 3
}

post {
  url: {{base_url}}/auth/refresh
  body: none
  auth: none
}

script:post-response {
  if (res.body.success) {
    bru.setEnvVar("access_token", res.body.data.access_token);
  }
}
